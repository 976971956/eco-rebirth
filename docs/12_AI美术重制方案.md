# AI 美术重制方案

版本：V1.4
适用范围：30 种物种、四生态区地图、Web / Android / iOS

## 1. 本次交付边界

本次使用 AI 生成两类资产：

1. 物种和生态地图的统一美术方向图，用来约束造型、色板、体块与细节密度；
2. 森林、草原、湿地、高地四张俯视手绘地表纹理，经过 1024×1024 压缩后直接进入 Godot 运行时。

当前环境不能直接生成带骨骼、动画、碰撞与移动端 LOD 的可交付 GLB，因此游戏中的 3D 动物并非把一张 AI 图片假装成模型，而是依据方向图重写 Godot 程序网格。这样能继续复用现有行走、攻击、技能、血条、AI、碰撞和存档逻辑，也能保持三端一致。

## 2. 资产清单

| 资产 | 用途 |
|---|---|
| `docs/images/v16-ai-species-direction.jpg` | 物种轮廓、配色、眼部和低多边形材质方向 |
| `docs/images/v16-ai-biome-kit.jpg` | 四生态区植被、岩石、水体和地面套装方向 |
| `assets/textures/terrain/forest_floor_ai.jpg` | 古木林地苔藓、土壤、根系地表 |
| `assets/textures/terrain/grassland_ai.jpg` | 日照草原短草、土点、野花地表 |
| `assets/textures/terrain/wetland_ai.jpg` | 浅水湿地泥地、苔藓、湿润斑块地表 |
| `assets/textures/terrain/highland_ai.jpg` | 岩丘高地碎石、地衣、枯草地表 |
| `docs/images/v16-species-v2.png` | Godot 实际物种渲染验收图 |
| `docs/images/v16-biome-kit-v2.png` | Godot 实际地图套装渲染验收图 |

四张运行时纹理均由内置 AI 图像生成模式制作，提示词共同约束为“正俯视、可平铺、手绘低多边形游戏地表、无方向光、无透视、无大型物件、无文字”。每张图再按生态区限定色彩和小型细节。原始生成图保留在本地 AI 生成目录，项目内只提交压缩后的运行版本。

## 3. 物种 V2 造型系统

`low_poly_factory.gd` 提供连续曲面、低面数球体、逐面颜色、地形和水体共享材质；`eco_actor.gd` 负责把它们组合为 30 种物种。

- 连续身体：躯干、颈部、头部与尾部优先使用连续截面曲面，减少相互穿插的球体；
- 清晰剪影：角、耳、喙、背甲、鳄甲、长尾和巨兽肩峰保持夸张但稳定；
- 面部层次：眼窝、虹膜、瞳孔和高光用于快速表达朝向与敌意；
- 逐面材质：每个三角面根据朝向与固定噪声获得轻微色差，在低面数下形成体积；
- 运动兼容：脚、翅、尾、身体根节点仍使用现有动画层级，视觉升级不改战斗半径；
- 特征强化：虎增加圆耳、颊毛、侧身条纹和尾环；鳄鱼改为连续长吻与成对背甲；熊强化肩峰、臀部和粗短四肢。

正式外包或后续 AI 3D 管线若替换某个物种，应逐只替换，不修改 `EcoActor` 公共接口。GLB 需使用 +Z 视觉朝向约定、脚底 Y=0、独立动画播放器，并在最低画质提供同等可读的简化版本。

## 4. 四生态区地图套装

| 区域 | 主色 | 大型轮廓 | 地表小物 |
|---|---|---|---|
| 古木林地 | 深绿、苔褐 | 盘根古树、分枝树冠 | 蕨类、菌类、倒木、落叶 |
| 日照草原 | 橄榄绿、暖黄 | 稀疏伞树、开阔视线 | 草簇、白黄野花、浅色土斑 |
| 浅水湿地 | 青绿、泥褐 | 水杉、低矮湿生植被 | 芦苇、香蒲、睡莲、浅水纹 |
| 岩丘高地 | 赭黄、冷灰 | 层叠松树、岩丘轮廓 | 碎石、地衣、枯草、砾土 |

四个区域仍在同一张可通行地图中，不增加加载门。道路使用分段弯曲的宽迁徙走廊，中心仍是战斗汇合点；区域过渡由不规则地表斑块完成。障碍碰撞继续只绑定真正阻路的树干和主岩石，小型花草、芦苇、碎石不制造隐形卡点。

## 5. 光照与水体

- 天空使用程序天空而不是纯色背景，并按昼夜、雨、雾、风暴切换色板；
- 主光负责方向和软化阴影，反向天空补光保留动物暗部细节；
- 薄雾只削弱远景，不遮蔽近距离战斗；
- 水体使用 Compatibility 可运行的轻量正弦波纹、浅深色混合和微弱高光，不使用屏幕空间反射；
- 性能档关闭主阴影并减少天气粒子，中高档保持同一地图内容与碰撞。

## 6. 三端性能规范

- AI 地表共四张 1024×1024 JPEG，由 Godot 导入后使用压缩和 mipmap；
- 单个动物以低面数程序网格和少量共享材质为主，不使用毛发、骨骼物理或高成本透明层；
- 区域装饰数量按画质档缩放，小型装饰无碰撞、无脚本、无每帧处理；
- 水和地表着色器只使用 Compatibility 支持的基础采样、噪声和正弦运算；
- Web 使用单线程导出，Android/iOS 沿用横屏触控、安全区和动态摇杆规则；
- 第 10 关需以 100 个个体完成运行测试，地图视觉升级不得降低最大体型的开放格连通率。

## 7. 后续制作顺序

1. 先按玩家使用频率选择 6 个代表物种制作正式绑定 GLB；
2. 在 Godot 中验证脚底、朝向、受击、攻击和技能挂点，再扩展同骨架物种；
3. 为每个生态区制作 3–5 个可拼装地块，不改变现有生成器的通行契约；
4. 增加近景/远景 LOD 和贴图图集，最后才提高纹理分辨率；
5. 每批资产都重新执行物种目录、关卡连通、1200 帧运行和 Web 导出验证。
